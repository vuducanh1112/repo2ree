"""Commands that build an REE: acquire, materialize, build, run, seal.

The author's lifecycle. These write to the REE tree itself and record author
receipts; the reviewer's mirrors in :mod:`..review` only ever read them.
"""
