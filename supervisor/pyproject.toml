[project]
name = "repo2ree-supervisor"
version = "0.1.0"
dependencies = [
    "repo2ree-protocol",
    "httpx~=0.28.1",
]

[tool.uv.sources]
repo2ree-protocol = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/repo2ree_supervisor"]
